name: Pipeline implementation

on: push

jobs:
  job1:
    name: First job
    runs-on: ubuntu-latest
    steps:
    - name: Step one
      uses: actions/checkout@v2
    - name: Step two
      run: env | sort
    - name: Docker command
      run: |
        docker start salesforce/cli:latest-rc-slim